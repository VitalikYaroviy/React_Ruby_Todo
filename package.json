{
  "name": "react-and-rails",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd my-app && npm install && npm run build && cd ..",
    "deploy": "cp -a my-app/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}